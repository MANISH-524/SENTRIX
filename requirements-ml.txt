# SENTRIX — optional full deep-learning profile (heavy).
# Everything degrades gracefully without these; install for the
# transformer log engine, LSTM anomaly detector, YOLO vision,
# time-series forecaster, and SLM LoRA training/inference.
torch>=2.4.0
torchvision>=0.19.0
transformers>=4.44.0
datasets>=2.20.0
sentence-transformers>=3.0.0
accelerate>=0.33.0
safetensors>=0.4.4
peft>=0.12.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.9.0
pandas>=2.2.0
statsmodels>=0.14.2
ultralytics>=8.3.0
opencv-python-headless>=4.10.0
Pillow>=10.4.0
